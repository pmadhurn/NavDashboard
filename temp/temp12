

```markdown
# ═══════════════════════════════════════════════════════════════
# NAVDASHBOARD — PHASE 5: PERSONNEL + INVENTORY (BACKEND ONLY)
# ═══════════════════════════════════════════════════════════════

## CONTEXT FROM PHASES 0–4

**Backend state:**
- FastAPI with auth (JWT), exception handlers, CORS
- Tables: users, audit_logs, devices, device_status_history, alembic_version
- Modules: auth, devices (full CRUD + status history + stats + seed)
- Available: get_db, Base, SoftDeleteMixin, CustomFieldsMixin, get_current_user, require_role, record_audit, paginate, apply_filters, PaginationParams, PaginatedResponse, model_to_dict, all exception classes
- Device model has `handling_person_id` (plain UUID, no FK yet) and `couple_id` (plain UUID, no FK yet)

**Frontend state:** Full layout shell, 14 shared components, devices module (list, detail, form, table). Not changed this phase.

**Docker:** nginx, frontend, backend, db (imresamu/postgis:16-3.5), redis. All healthy.

**Proven pattern from Phase 4:** constants → models → schemas → repository → service → router. This phase follows the same pattern exactly.

---

## PHASE 5 GOAL

After this phase:
- Personnel table exists with full CRUD API
- Assignment history tracking (who was assigned to what, when)
- Inventory tables exist: fitting_materials, material_templates
- Inventory has full CRUD plus template/copy/suggestion features
- Both modules tested via Swagger (`/docs`) — no frontend this phase
- Seed data for both modules via seed endpoints
- These modules are dependencies for Phase 6 (Couples) which needs personnel for handling_person and inventory for fitting materials

---

## FILES TO GENERATE

### Personnel module (NEW):
1. `NavDashboard/backend/modules/personnel/__init__.py`
2. `NavDashboard/backend/modules/personnel/models.py`
3. `NavDashboard/backend/modules/personnel/schemas.py`
4. `NavDashboard/backend/modules/personnel/repository.py`
5. `NavDashboard/backend/modules/personnel/service.py`
6. `NavDashboard/backend/modules/personnel/router.py`

### Inventory module (NEW):
7. `NavDashboard/backend/modules/inventory/__init__.py`
8. `NavDashboard/backend/modules/inventory/models.py`
9. `NavDashboard/backend/modules/inventory/schemas.py`
10. `NavDashboard/backend/modules/inventory/repository.py`
11. `NavDashboard/backend/modules/inventory/service.py`
12. `NavDashboard/backend/modules/inventory/router.py`
13. `NavDashboard/backend/modules/inventory/templates.py`

### Updates (REPLACE):
14. `NavDashboard/backend/main.py`
15. `NavDashboard/backend/migrations/env.py`

**Total: 15 files.**

---

## PERSONNEL MODULE SPECIFICATIONS

### models.py

**Person** model:
- Inherits: `Base`, `SoftDeleteMixin`, `CustomFieldsMixin`
- Table name: `personnel`
- Columns:
  - `full_name`: `Mapped[str]`, not nullable
  - `role`: `Mapped[str]`, not nullable (free text: "Technician", "Manager", "Installer", etc.)
  - `email`: `Mapped[Optional[str]]`, nullable
  - `phone`: `Mapped[Optional[str]]`, nullable
  - `notes`: `Mapped[Optional[str]]`, nullable

**AssignmentHistory** model:
- Inherits: `Base`
- Table name: `assignment_history`
- Columns:
  - `person_id`: `Mapped[UUID]`, FK to `personnel.id`, indexed
  - `entity_type`: `Mapped[str]` (device, couple, pair)
  - `entity_id`: `Mapped[UUID]`, indexed
  - `assigned_at`: `Mapped[datetime]`, `DateTime(timezone=True)`, `server_default=func.now()`
  - `unassigned_at`: `Mapped[Optional[datetime]]`, `DateTime(timezone=True)`, nullable

---

### schemas.py

```python
class PersonCreate(BaseModel):
    full_name: str = Field(min_length=1, max_length=200)
    role: str = Field(min_length=1, max_length=100)
    email: Optional[str] = None
    phone: Optional[str] = None
    notes: Optional[str] = None
    custom_fields: Optional[dict] = None

class PersonUpdate(BaseModel):
    full_name: Optional[str] = None
    role: Optional[str] = None
    email: Optional[str] = None
    phone: Optional[str] = None
    notes: Optional[str] = None
    custom_fields: Optional[dict] = None

class PersonResponse(BaseModel):
    id: UUID
    full_name: str
    role: str
    email: Optional[str] = None
    phone: Optional[str] = None
    notes: Optional[str] = None
    custom_fields: Optional[dict] = None
    created_at: datetime
    updated_at: Optional[datetime] = None
    model_config = {"from_attributes": True}

class AssignmentHistoryResponse(BaseModel):
    id: UUID
    person_id: UUID
    entity_type: str
    entity_id: UUID
    assigned_at: datetime
    unassigned_at: Optional[datetime] = None
    model_config = {"from_attributes": True}
```

---

### repository.py

Mandatory interface:
```python
async def get_by_id(db, id) -> Optional[Person]
async def get_multi(db, skip=0, limit=100, filters=None) -> list[Person]
async def create(db, obj_in: PersonCreate) -> Person
async def update(db, id, obj_in: PersonUpdate) -> Person
async def soft_delete(db, id) -> Person
```

Additional:
```python
async def search_by_name(db, query: str) -> list[Person]:
    """ILIKE search on full_name."""

async def get_assignments(db, person_id: UUID) -> list[AssignmentHistory]:
    """Current and past assignments for a person."""

async def create_assignment(db, person_id: UUID, entity_type: str, entity_id: UUID) -> AssignmentHistory:
    """Record a new assignment."""

async def end_assignment(db, person_id: UUID, entity_type: str, entity_id: UUID) -> Optional[AssignmentHistory]:
    """Set unassigned_at on the current assignment (where unassigned_at IS NULL)."""

async def count_all(db) -> int
```

---

### service.py

```python
async def list_personnel(db, params: PaginationParams, filters=None) -> PaginatedResponse[PersonResponse]
async def get_person(db, person_id: UUID) -> PersonResponse
async def create_person(db, person_in: PersonCreate, user_id: UUID) -> PersonResponse
async def update_person(db, person_id: UUID, person_in: PersonUpdate, user_id: UUID) -> PersonResponse
async def delete_person(db, person_id: UUID, user_id: UUID) -> PersonResponse
async def get_person_assignments(db, person_id: UUID) -> list[AssignmentHistoryResponse]
async def seed_personnel(db, user_id: UUID) -> list[PersonResponse]
```

**seed_personnel** creates 5 people (only if zero personnel exist):
```
Ahmed Al-Rashid, Technician, ahmed@example.com
Maria Santos, Installer, maria@example.com
James Chen, Manager, james@example.com
Fatima Noor, Technician, fatima@example.com
Lars Eriksson, Field Engineer, lars@example.com
```

All mutations call `record_audit`.

---

### router.py

Prefix: `/personnel` (mounted at `/api/v1/personnel`)

All endpoints require authentication.

```
GET    /                    → list personnel (paginated, filterable)
POST   /                    → create person
GET    /search?q=...        → search by name
GET    /{id}                → get person detail
PUT    /{id}                → update person
DELETE /{id}                → soft delete
GET    /{id}/assignments    → assignment history
POST   /seed                → seed sample data (admin only)
```

Query params for GET /: `page`, `size`, `full_name__contains`, `role`.

---

## INVENTORY MODULE SPECIFICATIONS

### models.py

**FittingMaterial** model:
- Inherits: `Base`, `SoftDeleteMixin`, `CustomFieldsMixin`
- Table name: `fitting_materials`
- Columns:
  - `couple_id`: `Mapped[Optional[UUID]]`, nullable (plain UUID, no FK yet — couples table created in Phase 6)
  - `name`: `Mapped[str]`, not nullable
  - `description`: `Mapped[Optional[str]]`, nullable
  - `quantity`: `Mapped[int]`, default 1
  - `unit`: `Mapped[Optional[str]]`, nullable (pcs, meters, kg, etc.)
  - `is_template`: `Mapped[bool]`, default False (if True, not tied to a specific couple — used as a reusable template item)

**MaterialTemplate** model:
- Inherits: `Base`
- Table name: `material_templates`
- Columns:
  - `template_name`: `Mapped[str]`, unique, not nullable
  - `description`: `Mapped[Optional[str]]`, nullable
  - `materials`: `Mapped[dict]`, JSONB, not nullable (list of material definitions: `[{"name": "...", "quantity": 1, "unit": "pcs"}, ...]`)

---

### schemas.py

```python
class MaterialCreate(BaseModel):
    couple_id: Optional[UUID] = None
    name: str = Field(min_length=1, max_length=200)
    description: Optional[str] = None
    quantity: int = Field(default=1, ge=1)
    unit: Optional[str] = None
    is_template: bool = False
    custom_fields: Optional[dict] = None

class MaterialUpdate(BaseModel):
    name: Optional[str] = None
    description: Optional[str] = None
    quantity: Optional[int] = None
    unit: Optional[str] = None
    custom_fields: Optional[dict] = None

class MaterialResponse(BaseModel):
    id: UUID
    couple_id: Optional[UUID] = None
    name: str
    description: Optional[str] = None
    quantity: int
    unit: Optional[str] = None
    is_template: bool
    custom_fields: Optional[dict] = None
    created_at: datetime
    updated_at: Optional[datetime] = None
    model_config = {"from_attributes": True}

class TemplateCreate(BaseModel):
    template_name: str = Field(min_length=1, max_length=200)
    description: Optional[str] = None
    materials: list[dict]  # [{"name": "...", "quantity": 1, "unit": "pcs"}, ...]

class TemplateUpdate(BaseModel):
    template_name: Optional[str] = None
    description: Optional[str] = None
    materials: Optional[list[dict]] = None

class TemplateResponse(BaseModel):
    id: UUID
    template_name: str
    description: Optional[str] = None
    materials: list[dict]
    created_at: datetime
    updated_at: Optional[datetime] = None
    model_config = {"from_attributes": True}

class SuggestionResponse(BaseModel):
    name: str
    count: int  # how many times this material name appears in the DB
```

---

### repository.py

Mandatory interface for FittingMaterial:
```python
async def get_by_id(db, id) -> Optional[FittingMaterial]
async def get_multi(db, skip=0, limit=100, filters=None) -> list[FittingMaterial]
async def create(db, obj_in: MaterialCreate) -> FittingMaterial
async def update(db, id, obj_in: MaterialUpdate) -> FittingMaterial
async def soft_delete(db, id) -> FittingMaterial
```

Additional:
```python
async def get_by_couple(db, couple_id: UUID) -> list[FittingMaterial]:
    """Get all materials for a specific couple."""

async def get_templates(db) -> list[MaterialTemplate]:
    """Get all material templates."""

async def get_template_by_id(db, template_id: UUID) -> Optional[MaterialTemplate]

async def create_template(db, obj_in: TemplateCreate) -> MaterialTemplate

async def update_template(db, template_id: UUID, obj_in: TemplateUpdate) -> MaterialTemplate

async def get_suggestions(db, query: str, limit: int = 10) -> list[dict]:
    """Autocomplete: find distinct material names matching query (ILIKE),
    with count of how many times each appears. Returns [{"name": "...", "count": N}]."""

async def count_all(db) -> int
```

---

### templates.py

Template and copy logic:

```python
async def create_materials_from_template(
    db: AsyncSession, template_id: UUID, couple_id: UUID,
) -> list[FittingMaterial]:
    """Load a template, create FittingMaterial entries for the couple
    based on the template's materials list."""

async def copy_materials_from_couple(
    db: AsyncSession, source_couple_id: UUID, target_couple_id: UUID,
) -> list[FittingMaterial]:
    """Copy all materials from one couple to another (suggestive completion).
    Creates new FittingMaterial entries with the same names/quantities/units."""
```

---

### service.py

```python
async def list_materials(db, params, filters=None) -> PaginatedResponse[MaterialResponse]
async def get_material(db, material_id) -> MaterialResponse
async def create_material(db, material_in, user_id) -> MaterialResponse
async def update_material(db, material_id, material_in, user_id) -> MaterialResponse
async def delete_material(db, material_id, user_id) -> MaterialResponse
async def get_materials_by_couple(db, couple_id) -> list[MaterialResponse]
async def list_templates(db) -> list[TemplateResponse]
async def create_template(db, template_in, user_id) -> TemplateResponse
async def update_template(db, template_id, template_in, user_id) -> TemplateResponse
async def create_from_template(db, template_id, couple_id, user_id) -> list[MaterialResponse]
async def copy_from_couple(db, source_couple_id, target_couple_id, user_id) -> list[MaterialResponse]
async def get_suggestions(db, query) -> list[SuggestionResponse]
async def seed_inventory(db, user_id) -> dict
```

**seed_inventory** creates (only if zero materials exist):
- 2 templates:
  - "Standard Rooftop Kit": [{"name": "Mounting Bracket", "quantity": 2, "unit": "pcs"}, {"name": "Ethernet Cable Cat6", "quantity": 1, "unit": "pcs"}, {"name": "Weatherproof Sealant", "quantity": 1, "unit": "tube"}, {"name": "Cable Ties", "quantity": 10, "unit": "pcs"}]
  - "Indoor Minimal Kit": [{"name": "Wall Mount Plate", "quantity": 1, "unit": "pcs"}, {"name": "Power Adapter", "quantity": 1, "unit": "pcs"}, {"name": "Short Patch Cable", "quantity": 1, "unit": "pcs"}]
- Returns `{"templates_created": 2}`

All mutations call `record_audit`.

---

### router.py

Prefix: `/inventory` (mounted at `/api/v1/inventory`)

All endpoints require authentication.

```
GET    /                           → list materials (paginated, filterable)
POST   /                           → create material
GET    /suggestions?q=...          → autocomplete material names
GET    /templates                  → list all templates
POST   /templates                  → create template
PUT    /templates/{template_id}    → update template
POST   /from-template/{template_id}?couple_id=... → create materials for couple from template
POST   /copy-from/{source_couple_id}?target_couple_id=... → copy materials between couples
GET    /by-couple/{couple_id}      → materials for a specific couple
GET    /{id}                       → get single material
PUT    /{id}                       → update material
DELETE /{id}                       → soft delete material
POST   /seed                       → seed templates (admin only)
```

Query params for GET /: `page`, `size`, `name__contains`, `couple_id`, `is_template`.

---

## UPDATED FILES

### main.py

Add to Phase 4 main.py:
1. Import: `from modules.personnel.router import router as personnel_router`
2. Import: `from modules.inventory.router import router as inventory_router`
3. Include: `app.include_router(personnel_router, prefix=settings.API_V1_PREFIX + "/personnel", tags=["personnel"])`
4. Include: `app.include_router(inventory_router, prefix=settings.API_V1_PREFIX + "/inventory", tags=["inventory"])`
5. Keep everything else unchanged.

Output the COMPLETE file.

---

### migrations/env.py

Add imports:
```python
from modules.personnel.models import Person, AssignmentHistory  # noqa
from modules.inventory.models import FittingMaterial, MaterialTemplate  # noqa
```

Keep all existing imports (AuditLog, User, Device, DeviceStatusHistory). Output the COMPLETE file.

---

## RULES

- Output ALL 15 files
- Output COMPLETE contents — no abbreviations, no stubs
- Follow the exact same code patterns established in Phase 4 devices module
- All queries filter `deleted_at IS NULL`
- All mutations call `record_audit`
- No frontend changes this phase
- No unused imports
- Proper async/await throughout

---

## OUTPUT FORMAT

### [full/path/to/filename.ext]

```[language]
[complete file contents]
```

---

## VERIFICATION STEPS

```bash
# Step 1: Rebuild backend
cd NavDashboard
docker compose up -d --build backend

# Step 2: Create and run migration
docker compose exec backend alembic revision --autogenerate -m "add_personnel_inventory"
docker compose exec backend alembic upgrade head

# Step 3: Verify tables
docker compose exec db psql -U navdashboard -d navdashboard_db -c "\dt"
# Expected: personnel, assignment_history, fitting_materials, material_templates
# (plus existing: users, audit_logs, devices, device_status_history)

# Step 4: Get auth token
TOKEN=$(curl -s -X POST http://localhost/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@navdashboard.com","password":"admin123"}' | \
  python3 -c "import sys,json; print(json.load(sys.stdin)['access_token'])")

# Step 5: Seed personnel
curl -X POST http://localhost/api/v1/personnel/seed \
  -H "Authorization: Bearer $TOKEN"
# Expected: list of 5 personnel

# Step 6: List personnel
curl http://localhost/api/v1/personnel \
  -H "Authorization: Bearer $TOKEN"
# Expected: PaginatedResponse with 5 items

# Step 7: Search personnel
curl "http://localhost/api/v1/personnel/search?q=ahmed" \
  -H "Authorization: Bearer $TOKEN"
# Expected: array containing Ahmed Al-Rashid

# Step 8: Create a person
curl -X POST http://localhost/api/v1/personnel \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"full_name":"Test Person","role":"Technician"}'
# Expected: created person response

# Step 9: Seed inventory
curl -X POST http://localhost/api/v1/inventory/seed \
  -H "Authorization: Bearer $TOKEN"
# Expected: {"templates_created": 2}

# Step 10: List templates
curl http://localhost/api/v1/inventory/templates \
  -H "Authorization: Bearer $TOKEN"
# Expected: 2 templates with materials arrays

# Step 11: Test suggestions
curl "http://localhost/api/v1/inventory/suggestions?q=mount" \
  -H "Authorization: Bearer $TOKEN"
# Expected: suggestions matching "mount" (Mounting Bracket, Wall Mount Plate)

# Step 12: Create material
curl -X POST http://localhost/api/v1/inventory \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"name":"Test Bracket","quantity":3,"unit":"pcs"}'
# Expected: created material response

# Step 13: Swagger docs show new endpoints
open http://localhost/docs
# Expected: /personnel and /inventory sections visible

# Step 14: Existing endpoints still work
curl http://localhost/api/v1/health
curl http://localhost/api/v1/devices/stats -H "Authorization: Bearer $TOKEN"
# Expected: both return correct responses

# Step 15: Audit trail has entries
docker compose exec db psql -U navdashboard -d navdashboard_db \
  -c "SELECT action, entity_type FROM audit_logs ORDER BY created_at DESC LIMIT 10;"
# Expected: entries for person and fitting_material entity types
```

**ALL 15 steps must pass.**

---

## TECHNICAL REPORT

After verification, produce concise report:
1. **Files created/modified** — count
2. **Database state** — new tables
3. **API surface** — new endpoints for both modules
4. **Seed data** — what was created
5. **Issues and fixes**
6. **Phase 6 needs** — what Couples + Locations module will use from personnel and inventory
```

madhur@Rajs-MacBook-Pro NavDashboard % tree
.
├── Makefile
├── backend
│   ├── Dockerfile
│   ├── __pycache__
│   │   └── main.cpython-312.pyc
│   ├── alembic.ini
│   ├── core
│   │   ├── __pycache__
│   │   │   ├── config.cpython-312.pyc
│   │   │   ├── database.cpython-312.pyc
│   │   │   ├── dependencies.cpython-312.pyc
│   │   │   ├── exceptions.cpython-312.pyc
│   │   │   └── security.cpython-312.pyc
│   │   ├── config.py
│   │   ├── database.py
│   │   ├── dependencies.py
│   │   ├── exceptions.py
│   │   ├── init.py
│   │   └── security.py
│   ├── main.py
│   ├── migrations
│   │   ├── __pycache__
│   │   │   └── env.cpython-312.pyc
│   │   ├── env.py
│   │   ├── init.py
│   │   ├── script.py.mako
│   │   └── versions
│   │       ├── 3845f6ceaac9_initial_audit_logs.py
│   │       ├── 8c160d3bc7f0_add_devices.py
│   │       ├── __pycache__
│   │       │   ├── 3845f6ceaac9_initial_audit_logs.cpython-312.pyc
│   │       │   ├── 5721eacc4a78_add_users_table.cpython-312.pyc
│   │       │   ├── 8c160d3bc7f0_add_devices.cpython-312.pyc
│   │       │   ├── ca3abdffb9ae_add_users_table.cpython-312.pyc
│   │       │   └── dd1090696ae2_initial_audit_logs.cpython-312.pyc
│   │       └── ca3abdffb9ae_add_users_table.py
│   ├── modules
│   │   ├── auth
│   │   │   ├── __pycache__
│   │   │   │   ├── models.cpython-312.pyc
│   │   │   │   ├── repository.cpython-312.pyc
│   │   │   │   ├── router.cpython-312.pyc
│   │   │   │   ├── schemas.cpython-312.pyc
│   │   │   │   └── service.cpython-312.pyc
│   │   │   ├── init.py
│   │   │   ├── models.py
│   │   │   ├── repository.py
│   │   │   ├── router.py
│   │   │   ├── schemas.py
│   │   │   └── service.py
│   │   ├── devices
│   │   │   ├── __pycache__
│   │   │   │   ├── constants.cpython-312.pyc
│   │   │   │   ├── models.cpython-312.pyc
│   │   │   │   ├── repository.cpython-312.pyc
│   │   │   │   ├── router.cpython-312.pyc
│   │   │   │   ├── schemas.cpython-312.pyc
│   │   │   │   └── service.cpython-312.pyc
│   │   │   ├── constants.py
│   │   │   ├── init.py
│   │   │   ├── models.py
│   │   │   ├── repository.py
│   │   │   ├── router.py
│   │   │   ├── schemas.py
│   │   │   └── service.py
│   │   └── init.py
│   ├── requirements.txt
│   └── shared
│       ├── __pycache__
│       │   ├── audit.cpython-312.pyc
│       │   ├── filters.cpython-312.pyc
│       │   └── pagination.cpython-312.pyc
│       ├── audit.py
│       ├── filters.py
│       ├── init.py
│       ├── pagination.py
│       └── utils.py
├── database
│   ├── backups
│   └── init
│       ├── 01_extensions.sql
│       └── 02_schemas.sql
├── docker-compose.yml
├── frontend
│   ├── Dockerfile
│   ├── index.html
│   ├── node_modules
│   ├── package.json
│   ├── src
│   │   ├── app
│   │   │   ├── App.tsx
│   │   │   ├── providers.tsx
│   │   │   └── routes.tsx
│   │   ├── main.tsx
│   │   ├── modules
│   │   │   ├── auth
│   │   │   │   ├── hooks
│   │   │   │   │   └── useAuth.ts
│   │   │   │   └── pages
│   │   │   │       └── LoginPage.tsx
│   │   │   ├── dashboard
│   │   │   │   └── pages
│   │   │   │       └── DashboardPage.tsx
│   │   │   └── devices
│   │   │       ├── components
│   │   │       │   ├── DeviceForm.tsx
│   │   │       │   └── DeviceTable.tsx
│   │   │       ├── hooks
│   │   │       │   └── useDevices.ts
│   │   │       └── pages
│   │   │           ├── DeviceDetailPage.tsx
│   │   │           └── DeviceListPage.tsx
│   │   ├── shared
│   │   │   ├── api
│   │   │   │   └── client.ts
│   │   │   ├── components
│   │   │   │   ├── ConfirmDialog.tsx
│   │   │   │   ├── DataTable.tsx
│   │   │   │   ├── EmptyState.tsx
│   │   │   │   ├── ErrorBoundary.tsx
│   │   │   │   ├── GlassButton.tsx
│   │   │   │   ├── GlassCard.tsx
│   │   │   │   ├── GlassInput.tsx
│   │   │   │   ├── GlassModal.tsx
│   │   │   │   ├── Layout.tsx
│   │   │   │   ├── LoadingSpinner.tsx
│   │   │   │   ├── PageHeader.tsx
│   │   │   │   ├── PlaceholderPage.tsx
│   │   │   │   ├── Sidebar.tsx
│   │   │   │   └── StatusBadge.tsx
│   │   │   ├── hooks
│   │   │   │   ├── useDebounce.ts
│   │   │   │   ├── useDevices.ts
│   │   │   │   └── useLocalStorage.ts
│   │   │   ├── stores
│   │   │   │   ├── authStore.ts
│   │   │   │   └── uiStore.ts
│   │   │   ├── types
│   │   │   │   ├── common.ts
│   │   │   │   └── devices.ts
│   │   │   └── utils
│   │   │       ├── colors.ts
│   │   │       ├── formatters.ts
│   │   │       └── validators.ts
│   │   ├── styles
│   │   │   ├── glass.css
│   │   │   ├── global.css
│   │   │   └── theme.ts
│   │   └── vite-env.d.ts
│   ├── tsconfig.json
│   ├── tsconfig.node.json
│   └── vite.config.ts
├── nginx
│   ├── Dockerfile
│   └── default.conf
└── temp
    ├── data
    ├── phase 1 report.md
    ├── phase 2 reports .ms
    ├── phase 3 report.md
    └── phase 4 report.md

43 directories, 119 files

this is the tree of the directory structure if you need to read a file you have 2 options ask me for important things from a file an ai will write a answer for you in less token using way or i can give you whole code if you want . 
and if you dont want to know about existing files just start writing code 